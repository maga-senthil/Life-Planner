@model IEnumerable<FamilyManagement.Models.DailyChore>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Chore.Chores)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Family.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ChoreDay)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Done)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Chore.Chores)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Family.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ChoreDay)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Done)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
